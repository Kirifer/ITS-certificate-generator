import { CertificateTitlePipe } from './certificate-title.pipe';

describe('CertificateTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new CertificateTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
